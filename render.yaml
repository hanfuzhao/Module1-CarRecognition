services:
  - type: web
    name: car-recognition
    runtime: python311
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PORT
        value: 5000
