# Tested with Python 3.13 on Apple-Silicon (MPS). Versions are lower-bounded;
# these are the exact versions used to produce the reported results.
torch>=2.6
torchvision>=0.21
scikit-learn>=1.4
scikit-image>=0.22
numpy>=1.26
pillow>=10.2
matplotlib>=3.8
datasets>=2.18
Flask>=3.0
gunicorn>=21.2
